// 注册监听器:
canvas.onmousedown = function (e) {
  // React to the mouse down event
};

// 使用更为通用的addEventListener()方法来注册监听器:
canvas.addEventListener(' mousedown', function (e) {
  // React to the mouse down event
});